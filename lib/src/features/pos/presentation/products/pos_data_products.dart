import 'package:flutter/material.dart';

class PosDataProducts extends StatelessWidget {
  const PosDataProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text(
          "Products",
        ),
      ],
    );
  }
}
