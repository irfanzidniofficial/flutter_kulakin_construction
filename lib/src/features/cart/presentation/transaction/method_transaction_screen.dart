import 'package:flutter/material.dart';

class MethodTransactionScreen extends StatelessWidget {
  const MethodTransactionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
